<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace= "com.dabeen.dnd.mapper.BsktMapper"> <!-- Mapper 클래스주소-->
    <insert id="insert" parameterType="com.dabeen.dnd.model.entity.Bskt" useGeneratedKeys="false">
        <selectKey order="BEFORE" keyProperty="bsktNum" resultType="String" > 
            <!-- 쿼리 못해먹겟다. 나중에 고쳐.. -->
            select concat(date_format(now(), "%y%m%d"), LPAD(ifnull(max(substr(bskt_num, 7)), "0") + 1, 3, 0))
            from bskt
            where substr(bskt_num, 1, 6) = date_format(now(), "%y%m%d") 
            <!-- 키 생성 쿼리 -->
        </selectKey>
        insert 
            into bskt
            values (#{bsktNum}, #{bsktUserNum}, #{totalPrice}, #{milegeUseWhet})
    </insert>
</mapper>

<!-- mybatis 작성파일 -->